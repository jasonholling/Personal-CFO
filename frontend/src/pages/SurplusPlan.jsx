import { useEffect, useState } from 'react'
import axios from 'axios'
import { isPrivacyMode, MASK_CURRENCY } from '../utils/privacy'
const fmt=n=>isPrivacyMode()?MASK_CURRENCY:new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0}).format(n||0)
const GOALS=['Emergency reserve','High-interest debt payoff','Retirement contributions','Education funding','Tax reserve','Taxable investing','Other goal']
export default function SurplusPlan(){const[d,setD]=useState(null);const[values,setValues]=useState({});const load=()=>axios.get('/api/surplus-allocations').then(r=>{setD(r.data);setValues(Object.fromEntries(r.data.allocations.map(x=>[x.goal,x.monthly_amount])))});useEffect(()=>{load()},[]);const save=async g=>{await axios.put(`/api/surplus-allocations/${encodeURIComponent(g)}`,{goal:g,monthly_amount:Number(values[g]||0)});load()};if(!d)return <div className="loading">Loading surplus plan...</div>;return <div><div style={{marginBottom:28}}><h1 className="section-title">Assign Your Surplus</h1><p className="section-sub">Turn monthly capacity into deliberate funding across your highest-priority goals.</p></div><div className="grid-3" style={{marginBottom:24}}><Metric label="Monthly surplus" value={fmt(d.monthly_surplus)}/><Metric label="Assigned" value={fmt(d.assigned)}/><Metric label="Still unassigned" value={fmt(d.unassigned)} color={d.unassigned<0?'var(--red)':d.unassigned===0?'var(--green)':'var(--amber)'}/></div><div className="card"><div style={{fontWeight:650,marginBottom:12}}>Monthly funding instructions</div>{GOALS.map(g=><div key={g} style={{display:'flex',gap:12,alignItems:'center',padding:'12px 0',borderTop:'1px solid var(--border)'}}><div style={{flex:1,fontSize:13}}>{g}</div><input aria-label={`${g} monthly allocation`} type="number" min="0" value={values[g]??''} onChange={e=>setValues(v=>({...v,[g]:e.target.value}))} style={{width:120,textAlign:'right'}}/><button className="btn-secondary" onClick={()=>save(g)}>Save</button></div>)}</div>{d.unassigned<0&&<div style={{color:'var(--red)',fontSize:13,marginTop:14}}>Assignments exceed the available monthly surplus. Reduce one or more amounts before treating this plan as funded.</div>}</div>}
function Metric({label,value,color}){return <div className="card"><div className="label">{label}</div><div className="number-lg" style={{marginTop:8,color}}>{value}</div></div>}
